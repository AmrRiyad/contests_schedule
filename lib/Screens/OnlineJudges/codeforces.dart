import 'package:flutter/material.dart' ;

class codeforces extends StatelessWidget {
  const codeforces({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Codeforces')),
    );
  }
}
